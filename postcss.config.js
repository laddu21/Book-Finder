// PostCSS configuration file

module.exports = {

    // Array of PostCSS plugins
    plugins: [

        // Tailwind CSS plugin
        require('tailwindcss'),

        // Autoprefixer plugin for CSS vendor prefixes
        require('autoprefixer'),

    ],

}